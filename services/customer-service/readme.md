# Customer Service

Customer profiles (`customer_db`). `POST /customers/validate` returns **400** with `data.valid=false` when field rules fail.

## Docker

Host port default **5003** → container **5000**.
